Config = {}

Config.Locale = 'en'

Config.LockDistance = 10.0
Config.GiveKeyDistance = 3.0

Config.Keys = {
    lock = 'g',
    menu = 'l',
}

Config.Commands = {
    lock = 'carkeys_lock',
    menu = 'carkeys_menu',
}

Config.Menu = {
    css = 'Your Header [when you dont use headers make a random word in]',
    align = 'top-left',
}

Config.Anim = {
    dict = 'anim@mp_player_intmenu@key_fob@',
    clip = 'fob_click',
    blendIn = 8.0,
    blendOut = -8.0,
    duration = -1,
    flag = 48,
}

Config.Sound = {
    name = 'Remote_Control_Fob',
    set = 'PI_Menu_Sounds',
}

Config.Lock = {
    unlocked = 1,
    locked = 2,
}

Config.PlateColor = '#43b6fe'

Config.DespawnCheckInterval = 3000
